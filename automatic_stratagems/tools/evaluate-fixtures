#!/usr/bin/env python3
"""Evaluate curated full-scene screenshots without recognition caching."""

from pathlib import Path
import sys


ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT))

from automatic_stratagems.fixture_evaluation import evaluation_main  # noqa: E402


raise SystemExit(evaluation_main())
